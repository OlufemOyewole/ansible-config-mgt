```
- name: update web, nfs, db and lb servers
  hosts: webservers, nfs, db, lb
  remote_user: ec2-user
  become: yes
  become_user: root
  tasks:
    - name: ensure wireshark is at the latest version
      yum:
        name: wireshark
        state: latest
        
```


# Thank you 